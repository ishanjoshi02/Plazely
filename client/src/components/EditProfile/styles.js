const styles = theme => ({
  card: {
    maxWidth: 200,
    margin: "32px auto 0px auto"
  },
  submitButton: {
    margin: "12px auto 0px auto",
    backgroundColor: "#2196F3"
  }
});

export default styles;
